# Статус реализации по AGENTS.md

**Дата:** 2026-07-20
**Проект:** ui-do (BN-Builder)
**Версия:** Vite 8.1 + React 19 + TS 6 + Tailwind v4 + shadcn/ui

---

## Майлстоуны (Раздел 10)

| # | Майлстоун | Статус | Проверка |
|---|---|---|---|
| M0 | **Скелет** — Vite+React+TS strict, Tailwind v4, shadcn, Vitest, layout редактора | ✅ | `tsc`, `lint`, `test` зелёные; `tailwind.config.*` нет |
| M1 | **Ядро** — ir.ts, registry.ts (22 компонента), tree.ts, store + zundo undo/redo | ✅ | Unit-тесты (insert/move/remove/patch, guard, undo/redo) |
| M2 | **Канвас** — рендер spec, выделение, инспектор, слои, Preview (375/768/1280), demo-фикстура | ✅ |
| M3 | **Drag-and-drop** — @dnd-kit, treeStrategy, индикаторы, canContain | ✅ |
| M4 | **Токены** — color.ts (hexToOklch + 6 тестов), useFontLoader, TokenInjector, UI редактора | ✅ |
| M5 | **Кодген** — codegen/generate.ts, codegen/logic.ts, LOGIC-блок, preserveLogic, data-bn-id | ✅ | 12 codegen-тестов (snapshot, LOGIC preservation, new events) |
| M6 | **Экспорт** — template/ (Vite+shadcn), scripts/export.ts | ✅ | `npx tsx scripts/export.ts --spec ... --out ...` → tsc validation ✅ |
| M7 | **Полировка** — хоткеи, localStorage, JSON import/export, управление экранами, empty states | ✅ |

**Итого: 8/8 майлстоунов выполнено.**

---

## Пораздельная сверка

### Раздел 0 — Миссия
- ✅ Детерминизм кодгена (вся итерация — явная сортировка)
- ✅ `data-bn-id` на каждом узле
- ✅ LOGIC-блок сохраняется при перегенерации (`preserveLogic`)
- ✅ Стили = данные: классы конкатенируются из IR; один `index.css` с токенами

### Раздел 1 — Стек
- ✅ Vite 8.1, React 19, TypeScript strict
- ✅ Tailwind v4 (`@tailwindcss/vite`, CSS-first)
- ✅ shadcn/ui (все компоненты)
- ✅ Zustand 5 + zundo 2
- ✅ @dnd-kit/core 6
- ✅ Vitest
- ✅ lucide-react

### Раздел 2 — Архитектура
- ✅ Единый источник правды — реестр (`registry.ts`)
- ✅ Схема: palette/canvas/inspector → Zustand → codegen → template/ → проект

### Раздел 3 — IR (`ir.ts`)
- ✅ Все типы совпадают со спецификацией (NodeId, ComponentNode, LayoutNode, TextNode, CodeNode, TokenSet, ScreenSpec, ProjectSpec)

### Раздел 4 — Реестр (`registry.ts`)
- ✅ ComponentDef, InspectorField
- ✅ 22 MVP-компонента
- ✅ canContain с правилами вложенности + unit-тесты

### Раздел 5 — Стор (`editor.ts`)
- ✅ Zustand + zundo (`temporal`, limit: 100)
- ✅ insertNode, moveNode, removeNode, patchNode, setTokens, select, setDropHint
- ✅ undo/redo через `useTemporalStore`
- ✅ mapNode для иммутабельных операций
- ✅ moveNode guard (запрет переноса в потомка)
- ✅ removeNode неприменим к root
- ✅ Тесты: insert/move/remove/patch, guard, undo/redo

### Раздел 6 — Канвас и DnD
- ✅ Рендер без обёрток (dnd-пропсы через спред)
- ✅ DndContext + PointerSensor (6px) + KeyboardSensor
- ✅ Draggable (палитра + узлы), Droppable (каждый узел)
- ✅ DragOverlay (чип + иконка)
- ✅ `treeStrategy` — кастомная collisionDetection
- ✅ DropHint в транзиент-сторе
- ✅ Индикаторы (top/bottom/inside)
- ✅ canContain = false → no-op

### Раздел 7 — Токены
- ✅ `color.ts` — hexToOklch + 6 unit-тестов
- ✅ `useFontLoader` — хук динамической подгрузки Google Fonts
- ✅ TokenInjector — live-применение токенов на канвасе
- ✅ UI: TokenColors, TokensPanel (color-picker, radius, шрифты)
- ✅ CSS-формат: `@import "tailwindcss"` + `:root` (oklch) + `@theme inline` + `@layer base`
- ✅ `--ring = var(--primary)`, `--radius` в rem

### Раздел 8 — Кодген и LOGIC
- ✅ `src/codegen/generate.ts` — `generateScreen(spec, opts?) → { tsx, css, htmlHead }`
- ✅ `src/codegen/logic.ts` — `defaultStubs(spec)` + `preserveLogic(prevTsx, nextTsx)`
- ✅ Импорты: алфавит по пути модуля, дубликаты слиты
- ✅ Порядок пропсов: `data-bn-id` → variants → props (алфавит) → `className` → events
- ✅ className: дефолт реестра + node.className, дедупликация, без `cn()`
- ✅ Одиночный TextNode — инлайн, иначе multiline
- ✅ Indent 2, singleQuote, semi, trailingComma all
- ✅ Шапка: `// GENERATED by BN-Builder · screen "{name}"`
- ✅ LOGIC-блок: `// ── BN:LOGIC:BEGIN ── ... ── BN:LOGIC:END ──`
- ✅ Тесты: snapshot LOGIN (Appendix B), preserveLogic, new event stubs
- ✅ Старый `export-code.ts` удалён, `ExportDialog.tsx` обновлён

### Раздел 9 — Экспорт
- ✅ `template/` — Vite + React + TS + Tailwind v4 + shadcn (22 компонента)
- ✅ template/package.json со всеми зависимостями + react-router-dom
- ✅ template/vite.config.ts с `@tailwindcss/vite` + `@/` alias
- ✅ template/ — **нет** `tailwind.config.js`
- ✅ `scripts/export.ts`:
  - ✅ `--spec <json> --out <dir>`
  - ✅ Копирование template/
  - ✅ `src/screens/*.tsx` (codegen)
  - ✅ `src/index.css` (oklch-токены)
  - ✅ `src/App.tsx` (react-router)
  - ✅ `index.html` (шрифты + title)
  - ✅ Валидация: `npx tsc --noEmit`
- ✅ Проверен end-to-end на тестовом spec (2 экрана)

### Раздел 11 — Не-цели
- ❌ Не делаем: бэкенд, БД, Sandpack, Figma-импорт, AI, тёмная тема, коллаборация — соблюдено

### Приложение D (Tailwind v4 чек-лист)
- ✅ Нет `tailwind.config.js` / `tailwind.config.ts` / `postcss.config.js`
- ✅ Нет `@tailwind base/components/utilities` — только `@import "tailwindcss"`
- ✅ `@tailwindcss/vite` в vite.config.ts
- ✅ Тема в CSS: `:root` + `@theme inline`
- ✅ Цвета в `oklch(...)`
- ✅ В IR нет arbitrary-значений (`w-[...]`)
- ✅ `tailwindcss` версии 4.x

---

## Финальные проверки (Правило 6)

| Команда | Статус |
|---|---|
| `npx tsc --noEmit` | ✅ 0 errors |
| `npm run lint` | ✅ 0 errors (6 pre-existing shadcn warnings) |
| `npm test` | ✅ 103/103 passed |

---

## Сводка рефакторинга

По результатам ревью кода проведены следующие исправления:

### Детерминизм
- Сортировка `Object.entries(colors)` по ключу в `generateCss` (оба цикла: `:root` и `@theme inline`)
- Замена `Math.random()` на `nanoid(8)` в `generateId()` и `wrapNode`
- ID больше не наследуются от родительских ID (устранён паттерн `nodeId + '_wrap'`)

### Типизация и валидация
- Убраны все `id: '' as never` из реестра компонентов
- Добавлена фабрика `createNode()` с автоматическим присвоением nanoid всем потомкам
- Добавлен `HEX_RE` и валидация в `hexToOklch` (throw на невалидный hex)
- Добавлена функция `validateProjectSpec()` для проверки загружаемых JSON-спецификаций

### Компоненты и вложенность
- `Select` сделан составным (`isContainer: true`) с подкомпонентами SelectTrigger/Value/Content/Item
- Расширены правила `canContain` для Select-семейства
- Добавлен `CanvasErrorBoundary` для защиты канваса от ошибок рендера
- Добавлен атрибут `data-bn-direction` для корректного определения направления layout в DnD

### Инфраструктура
- Добавлен Prettier с конфигурацией (репозиторий + template)
- Отформатированы все 70+ файлов
- Обновлён README.md (вместо дефолтного Vite-шаблона)
- Добавлены CSS-переменные: `--card-foreground`, `--secondary`, `--popover`, `--chart-1..5`

### Тесты (73 → 103)
- 4 теста hexToOklch (валидация)
- 10 тестов canContain (Select-семейство)
- 5 тестов createNode (фабрика, ID, subtree уникальность)
- 1 тест wrapNode ID independence
- 3 теста generateCss (детерминизм, сортировка)
- 9 тестов validateProjectSpec (граничные случаи, валидная спека)

---

## Структура проекта

```
ui-do/
├── AGENTS.md                 — Рабочий документ агента
├── AGENTS-STATUS.md          — Данный файл (сводка реализации)
├── template/                 — Vite-шаблон для экспорта (M6)
│   ├── package.json
│   ├── vite.config.ts
│   ├── tsconfig*.json
│   ├── index.html
│   └── src/
│       ├── main.tsx
│       ├── lib/utils.ts
│       └── components/ui/    — 22 shadcn-компонента
├── scripts/
│   └── export.ts             — Скрипт экспорта проекта (M6)
├── src/
│   ├── core/
│   │   ├── ir.ts             — Доменная модель (M1)
│   │   ├── registry.ts       — Реестр 22 компонентов (M1)
│   │   ├── tree.ts           — Операции над деревом (M1)
│   │   ├── color.ts          — hexToOklch (M4)
│   │   ├── dnd-strategy.ts   — Кастомная collisionDetection (M3)
│   ├── codegen/
│   │   ├── generate.ts       — generateScreen() (M5)
│   │   ├── logic.ts          — preserveLogic + defaultStubs (M5)
│   ├── store/
│   │   └── editor.ts         — Zustand + zundo (M1)
│   ├── components/
│   │   ├── Canvas.tsx        — Канвас с рендером (M2)
│   │   ├── CanvasRenderer.tsx— Рекурсивный рендер (M2)
│   │   ├── Inspector.tsx     — Инспектор свойств (M2)
│   │   ├── LayersPanel.tsx   — Панель слоёв (M2)
│   │   ├── Palette.tsx       — Палитра компонентов (M3)
│   │   ├── Editor.tsx        — Главный редактор (M0)
│   │   ├── ExportDialog.tsx  — Диалог экспорта (M5)
│   │   ├── TokenColors.tsx   — Редактор цветов (M4)
│   │   ├── TokensPanel.tsx   — Панель токенов (M4)
│   │   ├── TokenInjector.tsx — Применение токенов (M4)
│   │   ├── ProjectMenu.tsx   — Save/Load/New (M7)
│   │   ├── ScreenTabs.tsx    — Управление экранами (M7)
│   │   ├── ContextMenu.tsx   — Контекстное меню (дополнительно, вне спеки)
│   │   ├── UndoRedo.tsx      — undo/redo UI (M7)
│   │   ├── DragOverlayContent.tsx — (M3)
│   │   ├── DropIndicators.tsx — (M3)
│   │   ├── ComponentMap.tsx  — (M3)
│   │   └── ui/               — shadcn/ui компоненты
│   ├── hooks/
│   │   ├── useFontLoader.ts  — Загрузка Google Fonts (M4)
│   │   └── useKeyboard.ts    — Хоткеи (M7)
│   ├── fixtures/
│   │   └── demo-spec.ts      — Фикстура из Appendix A (M2)
│   └── lib/
│       └── utils.ts          — cn()
└── examples/
    └── test-project.json     — Тестовый spec для export.ts
```

---

## Итог

**Все 8 майлстоунов AGENTS.md реализованы полностью.**\
Проект проходит `tsc --noEmit`, `npm run lint` (0 errors) и `npm test` (103/103).
